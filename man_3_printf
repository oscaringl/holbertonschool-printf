.TH printf 3 "July 4, 2023" "Oscar Guerra"

.SH NAME
printf - format and print data

.SH SYNOPSIS
#include "main.h"

int _printf(const char *format, ...);

.SH DESCRIPTION
The _printf function formats and prints data to the standard output.

.SH RETURN VALUE
The _printf function returns the number of characters printed (excluding the null byte used to end output to strings).

.SH FORMAT SPECIFIERS
The following format specifiers are supported:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a string of characters.

.TP
.B %d, %i
Prints a signed decimal integer.

.TP
.B %u
Prints an unsigned decimal integer.

.TP
.B %o
Prints an unsigned octal integer.

.TP
.B %x
Prints an unsigned hexadecimal integer using lowercase letters (a-f).

.TP
.B %X
Prints an unsigned hexadecimal integer using uppercase letters (A-F).

.TP
.B %b
Prints an unsigned integer in binary format.

.TP
.B %p
Prints a pointer address.

.TP
.B %R
Prints a string in rot13 format (only uppercase letters).

.TP
.B %r
Prints a string in reverse.

.SH EXAMPLES
.B _printf("Hello, %s!\n", "world");
Prints: Hello, world!

.B _printf("The value is %d\n", 42);
Prints: The value is 42

.SH AUTHOR
Written by Oscar Guerra.

.SH SEE ALSO
printf(3), putchar(3), puts(3)

.SH BUGS
No known bugs.
